# Vite (React SPA) — copied to .env.local by pocket-agent bootstrap
VITE_AUTH_MODE=none
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_CONNECTION_PROFILE=all-local
# Same-origin API in monorepo dev (omit or leave empty). Override for split deploys.
# VITE_API_BASE_URL=
